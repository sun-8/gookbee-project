<?xml version="1.0" encoding="UTF-8"?>

<!-- http://www.mybatis.org/mybatis-3/ko/sqlmap-xml.html 참고 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.zerock.mapper.TimeMapper">
  
  	<!-- 
  	select 태그의 id 속성의 값은 메서드의 이름과 동일하게 맞춰야 한다. 
  	select 태그의 경우 resultType 속성을 가지는데 이 값은 인터페이스에 선언된 메서드의 리턴 타입과 동일하게 작성한다.
  	-->
  	<select id="getTime2" resultType="string">
  		select sysdate from dual
  	</select>
  
  </mapper>
  
  
  <!-- 
  XML을 작성해서 사용할 때는 XML파일의 위치와 XML파일에 지정하는 namespace속성이 중요한데,
  XML파일 위치의 경우 mapper 인터페이스가 있는 곳에 같이 작성하거나 src/main/resources 구조에 XML을 저장할 폴더를 생성할 수 있다.
  XML파일을 만들 때 이름에 대한 규칙은 없지만, 가능하다면 Mapper 인터페이스와 같은 이름을 사용하는 것이 가독성을 높여준다.
  XML 매퍼를 이용할 때 신경 써야 하는 부분은 mapper 태그의 namespace 속성값이다.
  MyBatis는 Mapper 인터페이스와 XML을 인터페이스의 이름과 namespace 속성값을 가지고 판단한다.
  org.zerock.mapper.TimeMapper 인터페이스가 존재하고, XML의 <mapper namespace="org.zerock.mapper.TimeMapper">와 같이 동일한 이름이 존재하면 이를 병합해서 처리한다.
  따라서 위의 경우 메서드 선언은 인터페이스에 존재하고 SQL에 대한 처리는 XML을 이용하는 방식이라고 볼 수 있다.
   -->
  